@using UniqueTrade_App.CommanFunction;
@model IEnumerable<UniqueTrade_App.Models.UserLevelModel>
@{
    UserInfo userInfo = Common.CurrentUserInfo;
    ViewBag.Title = "TeamDirectReport";
    Layout = "~/Views/Shared/_User_Layout.cshtml";
}


<!--#INCLUDE FILE= "header.asp"-->
<!--**********************************
    Content body start
***********************************-->
<div class="content-body">
    <!-- row -->
    <div class="container-fluid">
        <div class="row page-titles">
            <ol class="breadcrumb">
                <li class="breadcrumb-item active"><a href="">Direct Team</a></li>
                <li class="breadcrumb-item"><a href="/MemberPanel/index">Home</a></li>
            </ol>
        </div>
        <!-- row -->
        <div class="row">
            <div class="col-xl-12">
                <div class="card">
                    <div class="card-header">
                        <h4 class="card-title">Direct Team</h4>
                    </div>
                    <div class="card-body">
                        <div class="table-responsive">
                            <table id="example" class="display" style="min-width: 845px">
                                <thead>
                                    <tr>
                                        <th>Sr.No</th>
                                        <th>joining Date</th>
                                        <th>User ID</th>
                                        <th>User Name</th>
                                        <th>Email ID</th>
                                        <th>Mobile Number</th>
                                        <th>Status</th>
                                    </tr>
                                </thead>
                                <tbody>
                                    @{ int rows = 0;}
                                    @foreach (var item in Model)
                                    {
                                        <tr>
                                            <td>@(rows = rows + 1)</td>
                                            <td>@item.reg_date</td>

                                            <td>@item.username</td>
                                            <td>@item.memb_name</td>

                                            @*<td>BUSD @item.Total_Business</td>
                <td>@item.credit_date</td>*@
                                            <td>@item.email</td>
                                            <td>@item.mobile_number</td>
                                            <td>
                                                @if (@item.ac_status == "G")
                                                {
                                                    <h5 class="text-white">Active</h5>
                                                }
                                                else
                                                {
                                                    <span>Inactive</span>
                                                }
                                            </td>

                                        </tr>
                                    }
                                </tbody>
                            </table>
                        </div>
                    </div>
                </div>
            </div>
        </div>
    </div>
</div>








